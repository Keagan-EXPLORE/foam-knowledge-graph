## Convert string to uppercase
```
def convert(string):
    return string.upper())
```

## Add 2 spaces between each letter in a string
```
def spaces(string):
    return "  ".join(s.replace(" ", ""))
```